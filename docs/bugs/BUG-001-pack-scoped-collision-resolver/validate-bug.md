# Validate Bug — BUG-001: Pack-scoped collision resolver

## 1. Target And Fingerprint

- Command: `validate bug BUG-001`
- Bug record: `docs/bugs/BUG-001-pack-scoped-collision-resolver/bug-report.md`
- Code surfaces: `.prism/core/tools/seal_sprint.py`, `.prism/core/tools/test_seal_sprint.py`
- HEAD: `479795ba6a7df3e13e0d70541d5c2d938765dbda`
- Bug-record fingerprint: `sha256:faaba5994262`
- Fix-delta fingerprint: `sha256:8b227c261d54`
- Runtime file fingerprints: `seal_sprint.py sha256:e4fc76c42ed8`; `test_seal_sprint.py sha256:711b4d3cc466`
- Timestamp (UTC): `2026-07-19T12:11:55Z`
- Independent conduct: a fresh-context reviewer re-derived the contract from the Bug Manager, Change Manager, CLI help, bug record, and current files; the main audit independently re-ran the repro and tests.

## 2. Expectation Sources And Structural Coverage (`VAL-1`, `VAL-2`)

| Source / surface | Expected evidence | Result |
|---|---|---|
| `.prism/core/bug-manager.md` | reproducible defect, expected/actual, allowed boundary, doc impact, tagged regression | Pass |
| `.prism/core/change-manager.md § Same-Sprint Collision Check` | full id/version/slug resolve a canonical pack; missing/ambiguous selector exits 2 | Pass |
| `seal_sprint.py --help` | same public selector and exit-code contract | Pass |
| Bug record | minimum fields and scope declared before code edit | Pass |
| Code delta | only declared resolver and regression-test surfaces | Pass |
| Regression evidence | `VERIFIES: BUG-001`, original repro no longer fails, refutation branches pass | Pass |

## 3. Rule Coverage

| Rule ID / gate | Result | Evidence |
|---|---|---|
| `VAL-1` | Pass | Fingerprints, coverage, semantic evidence, findings, and conclusion recorded |
| `VAL-2` | Pass | Fresh eyes, re-derived expectation, repro-before/fix-after refutation, and independent reruns |
| `ORIGIN-1` / D-F | Pass | Existing documented behavior was violated; this is a defect, not a new requirement |
| Reproduction | Pass | Pre-fix implementation exits 2; fixed implementation exits 0 on the exact repository command |
| Regression test | Pass | Five tagged tests pass and cover identity, ambiguity, missing selector, unrelated collision, and participating collision |
| `CODE-5` / diff boundary | Pass | No code/test change exists outside the two declared files; generated bytecode was cleaned |
| Doc impact | Pass | `none` is supported because the specification already states the intended behavior |
| `SEM-1` | Pass | No contract weakening, bypass, duplicated resolver meaning, terminology drift, stale assumption, or out-of-scope behavior |
| Code quality | Pass | Directory-owned identity is centralized and reused by overlap and structure checks; deterministic tests use temporary fixtures |

## 4. Semantic Integrity (`SEM-1`)

| Area | Evidence checked | Finding | Severity | Required action |
|---|---|---|---|---|
| Selector contract | Full id, version, slug, missing, and ambiguous selectors | Matches documented behavior | Info | None |
| Collision safety | Unrelated and participating synthetic collisions | Scoping excludes unrelated collisions and still blocks a collision involving the selected pack | Info | None |
| Plan-only behavior | Exact v1.7.20 repository pack | Pack resolves without requiring mergeable delta files | Info | None |
| Doc impact | Change Manager and CLI help | Existing docs are correct; no governed artifact change required | Info | None |
| Scope | Git diff/status and allowed boundary | No out-of-boundary code/test or generated bytecode remains | Info | None |

## 5. Runtime Evidence

| Command / refutation | Result |
|---|---|
| Pre-fix implementation against exact repository repro | FAILS as reproduced — exit 2, `matched no change pack` |
| `PYTHONDONTWRITEBYTECODE=1 python3 .prism/core/tools/test_seal_sprint.py` | PASS — 5/5 |
| Exact pack-scoped collision command after fix | PASS — exit 0, no collision |
| Scoped version and slug selectors | PASS — exit 0 |
| Synthetic missing and ambiguous selectors | PASS — exit 2 |
| Synthetic unrelated collision | PASS — selected Plan-only pack exits 0 while unscoped sweep exits 1 |
| Synthetic collision involving selected pack | PASS — exit 1 and reports the anchor |
| `git diff --check` | PASS |
| `__pycache__` / `.pyc` status | PASS — no generated diff |

## 6. Findings

| Severity | Rule ID | Location | Finding | Required fix |
|---|---|---|---|---|
| warn | Git hygiene | Current branch `feature/tg-01-shared-contracts` | Bug Manager prescribes `fix/BUG-001-pack-scoped-collision-resolver`; existing unrelated dirty implementation work made branch isolation unsafe in this turn. This does not invalidate behavior or scope evidence. | Move the bug changes to the prescribed fix branch before commit if repository history must isolate the bug |
| info | Code cleanup | `.prism/core/tools/seal_sprint.py` | `source_matches_pack()` is now unused after directory-owned resolution | Optional future cleanup; no behavior or validation impact |

## 7. Conclusion

- blocker: 0
- warn: 1
- info: 1
- latest conclusion: `clean`
